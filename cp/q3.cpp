#include <iostream>
#include <vector>
using namespace std;

string func(int x,string s){
    
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;
    vector<int> v;
    while (t--)
    {
        int n;
        cin>>n;
        s
    }
    for (int i : v)
    {
        cout << i << endl;
    }
}
